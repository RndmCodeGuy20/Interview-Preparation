name: Use Daily LeetCode Problem Workflow

on:
  schedule:
    - cron: "0 1 * * *"

jobs:
  use-daily-lc-problem:
    runs-on: ubuntu-latest
    steps:
      - name: Get Daily LeetCode Problem
        uses: RndmCodeGuy20/Daily-LC-Problem-Action@v1.1.0
