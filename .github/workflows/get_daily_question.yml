name: Use Daily LeetCode Problem Workflow

on:
  schedule:
    - cron: "0 1 * * *"
  workflow_dispatch:

jobs:
  use-daily-lc-problem:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    - name: Get LeetCode Problem
      uses: RndmCodeGuy20/Daily-LC-Problem-Action@master
    - name: Commit and push changes
      run: |
        git config --global user.name 'RndmCodeGuy20'
        git config --global user.email 'shantanu.mane.200@outlook.com'
        git status
        git add -A
        git commit -m "chore(action) 🦈: today's daily leetcode question added"
        git push
