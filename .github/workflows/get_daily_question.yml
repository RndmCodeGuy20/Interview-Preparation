name: Use Daily LeetCode Problem Workflow

on:
  schedule:
    - cron: "0 1 * * *"
  workflow_dispatch:

jobs:
  use-daily-lc-problem:
    runs-on: ubuntu-latest
    steps:
      - name: Get LeetCode Problem
        uses: RndmCodeGuy20/Daily-LC-Problem-Action@v1.3.0
